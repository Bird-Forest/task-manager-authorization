export const statusFilters = Object.freeze({
  all: 'all',
  red: '#ff5252',
  yellow: '#ffee58',
  green: '#00e676',
  // important,
  // priority,
  // minor,
  // secondary,
});
console.log(statusFilters);

// export const statusFilters = Object.freeze({
//   all: 'all',
//   active: 'active',
//   completed: 'completed',
// });
// console.log(statusFilters);
